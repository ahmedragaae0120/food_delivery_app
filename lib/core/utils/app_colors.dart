import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(
    0xFFFA4A0C,
  ); // البرتقالي الأساسي في التصميم
  static const Color primaryLight = Color(0xFFFFE1CE);
  static const Color primaryDark = Color(0xFFE25E14);

  // ألوان الخلفية والأسطح
  static const Color background = Color(0xFFF2F2F2);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(
    0xFF181C2E,
  ); // الأسود المائل للكحلي المستخدم في النصوص

  // ألوان النصوص والرمادي
  static const Color textPrimary = Color(0xFF181C2E);
  static const Color textSecondary = Color(0xFF676767);
  static const Color grey = Color(0xFFA0A5BA);
  static const Color greyLight = Color(0xFFE8EAED);

  // ألوان الحالة
  static const Color error = Color(0xFFE23E3E);
  static const Color success = Color(0xFF059C6A);
  static const Color warning = Color(0xFFFFAD1D);
}
